let package = Package(
    // ...
    dependencies: [
        //// Option 1: Track the tip of the main branch. This follows branch updates over time and is not pinned to SemVer-tagged releases.
        .package(url: "https://github.com/microsoft/clarity-apps", branch: "main")
        //// Option 2: Use SemVer-tagged releases from 3.0.0 up to, but not including, 4.0.0 (that is, compatible 3.x updates).
        // .package(url: "https://github.com/microsoft/clarity-apps", from: "3.0.0")
    ],
    targets: [
        .target(
            name: "<target-name>",
            dependencies: [
                .product(name: "Clarity", package: "clarity-apps")
            ]
        )
    ]
)
